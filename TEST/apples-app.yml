apiVersion: v1
kind: Secret
metadata:
  name: mongodb-credentials
type: Opaque
data:
  username: 77u/YWRtaW4NCg==
  password: 77u/UEBzc3cwcmQxDQo=

---

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: apples-app
  name: apples
spec:
  replicas: 2
  selector:
    matchLabels:
      name: apples-app
  template:
    metadata:
      labels:
        name: apples-app
    spec:
      containers:
      - image: node:latest
        name: apples-app
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: production
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongodb-credentials
              key: password
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: mongodb-credentials
              key: username
        - name: PORT
          value: "3000"
        - name: MONGODB_URI
          value: "mongodb://$MONGO_INITDB_ROOT_USERNAME:$MONGO_INITDB_ROOT_PASSWORD@mongo:27017/application"
        #workingDir: /tmp/
        #command: ["/bin/bash", "-c", "node server.js && npm run start && npm ls --depth=0"]
        command: [ "/bin/bash", "-c", "$MOGODB_URI && while true; do sleep 30; done;" ]
        args: [ "$MOGODB_URI" ]
        volumeMounts:
        #- name: node-modules
        #  mountPath: /app/node_modules
        - name: appvol
          mountPath: /app
      volumes:
      - name: node-modules
        emptyDir: {}
      - name: appvol
        hostPath:
          path: "C:/Users/Administrator.KOBI-LAT5480/Documents/Provisioning/TEST/app/"
          type: Directory